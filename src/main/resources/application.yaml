server:
  port: 8080
  servlet:
    context-path: /infinity

spring:
  datasource:
    url: ${DBMS_CONNECTION:jdbc:mysql://localhost:3306/infinity_crm}
    driverClassName: "com.mysql.cj.jdbc.Driver"
    password: ${DBMS_PASSWORD:root}
    username: ${DBMS_USERNAME:root}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
jwt:
  signerKey: "LWqlzG1wE66gSkkzE3TSmL+1OFuc+d7BrrVdMnjLtWqSroJFKZq8fiNVHRuDcd4Y\n"
  valid-duration: 3600
  refreshable-duration: 86400

admin:
  default-password: "admin"
